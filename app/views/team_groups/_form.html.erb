<%= form_for(@team_group) do |f| %>
  <% if @team_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team_group.errors.count, "error") %> prohibited this team_group from being saved:</h2>

      <ul>
      <% @team_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :namecode %><br />
    <%= f.text_field :namecode %>
  </div>
  <div class="field">
    <%= f.label :activated %><br />
    <%= f.check_box :activated %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
