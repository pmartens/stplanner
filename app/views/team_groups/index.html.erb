<h2>Listing team groups</h2>

<table>
  <tr>
    <th>Name</th>
    <th>Namecode</th>
    <th>Activated</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @team_groups.each do |team_group| %>
  <tr>
    <td><%= team_group.name %></td>
    <td><%= team_group.namecode %></td>
    <td><%= team_group.activated %></td>
    <td><%= link_to 'Show', team_group %></td>
    <td><%= link_to 'Edit', edit_team_group_path(team_group) %></td>
    <td><%= link_to 'Destroy', team_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Team group', new_team_group_path %>
